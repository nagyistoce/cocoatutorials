#!/bin/csh

find . -name .DS_Store -delete
echo "------- links -------------"
find "${1}" -type l -exec p4 add -t symlink {} \;
echo "------- files -------------"
find "${1}" -type f -exec p4 add {} \;
echo "-------  end  -------------"
