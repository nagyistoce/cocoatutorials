#!/bin/bash

if [ $# -ne 1 ];then
	echo pingz servername
	exit 1
fi

ping="$@"
st=1
while [ $st -ne 0 ]; do
	ping -c 1 $ping 2>&1 > /dev/null
	st=$?
	if [ $st -ne 0 ]; then
		echo $ping is DEAD
		sleep 1	
	fi
	ping -c 1 $ping | egrep -e bytes # -e loss
done

# That's all Folks!
##
