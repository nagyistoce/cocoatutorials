#!/bin/bash

# if there an extra argument, delete this script and the launch daemon which called it
if [ $# -gt 0 ]; then
	echo "yes, there is stuff on the command line"
	dir=`cd $0; pwd`
	b=`basename $0`
	echo I am $dir/$b
else
	echo I do not have an argument with that
fi	

