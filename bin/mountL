#!/bin/csh

ls /L/windows >& /dev/null
if ( $status ) then
	echo "/L isn't mounted.  Hang on."
	osascript -e 'mount volume "smb://rmills-laptop;rmills:Largs12%40@rmills-laptop/C"'
else
	echo '/L is mounted'
endif


# df -k | grep -e Volumes -e Filesystem
