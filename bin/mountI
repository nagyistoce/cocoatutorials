#!/bin/csh

if ( ! -e /I ) then
	ln -s /Volumes/I /iMac
endif

ls /I/etc >& /dev/null
if ( $status ) then
	echo "/I isn't mounted.  Hang on."
	osascript -e 'mount volume "afp://rmills:Largs12%40@iMac/iMacHD"'
else
	echo '/I is mounted'
endif


# df -k | grep -e Volumes -e Filesystem
