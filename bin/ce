#!/bin/bash

if [ $(uname) == 'Darwin' ]; then
	bbedit "$@"
elif [ $(uname) == 'Linux' ]; then
	kate --use "$@" 2> /dev/null > /dev/null &
elif [ $(uname -o) == 'Cygwin' ]; then
	/cygdrive/c/Users/rmills/com/ce.exe "$@"
else
	vi "$@"
fi

# That's all folks
##
